name: example
description: A project used to demo the Todo Reporter
version: 1.0.0+2

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  http: ^1.1.0
  nimbus4flutter: ^0.0.2
  nimbus_annotation: ^1.0.0+4

dev_dependencies:
  build_runner: ^2.4.6
  nimbus_generator: 
    path: ../

dependency_overrides:
  http: ">=0.13.1"
